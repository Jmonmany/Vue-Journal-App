export const getEntriesBtTerm = (state) => {
  return state;
};
export const getEntryById = (state) => {
  return state;
};
// getters are functions that will return a value from the state in the form of a computed property
